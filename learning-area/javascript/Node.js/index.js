var klass = require("./klass");

// exports.add = function(klasses) {
//     klasses.forEach(element => {
//         klass.add(element.teacherName, element.students);
//     });

// };

klass.add("s", ["a", "b"])